import { FC } from "react";

import Layout from "../../components/Layout";

const MainPage: FC = () => {
  return (
    <Layout>
      <div>Главная</div>
    </Layout>
  );
};

export default MainPage;
